% Check if required metadata can be found
\requirecommand{university}
\requirecommand{keywords}

\requirecommand{paperTitle}
\requirecommand{paperDate}

\requirecommand{paperAuthors}
\requirecommand{paperEmails}

% Metadata
\title{\paperTitle}
\author{
	\paperAuthors \\
	\normalsize \texttt{\paperEmails} \\
	\normalsize \university
}
\date{\paperDate}
% > Add PDF metadata
\hypersetup{
	% Colorize all links/anchors blue (for digital distribution)
	hidelinks=false,
	colorlinks=true,
	linkcolor=blue,
	urlcolor=blue,
	citecolor=blue,
	% Metadata
	pdfauthor={\paperAuthors},
	pdftitle={\paperTitle},
	pdfsubject={\university},
	pdfkeywords={\keywords}
}
