\documentclass{article}				% -> Article layout (default)
%\documentclass{scrartcl}			% -> Modern/Bigger article layout (recommended)
%\documentclass[journal]{IEEEtran}	% -> Two column journal layout
%\documentclass{report}				% -> Report layout
% ==========================================================================

% Add packages to expand the functionality with the following command:
% \usepackage[options]{packagename}
% ==========================================================================


\begin{document}
	Hello World! \\
	(page Numbers are automatically added, when using the 'article' class) \\
	here you can paste or/and write everything you want to have in your Document.
\end{document}